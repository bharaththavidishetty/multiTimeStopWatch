import * as React from 'react';
import Svg, {SvgProps, Circle, Path, G} from 'react-native-svg';
const CoinIcon = (props: SvgProps) => (
  <Svg width={30} height={30} viewBox="0 0 512 512" {...props} />
);
export default CoinIcon;
